// Configuração da API em runtime
// Este arquivo pode ser modificado após o build para configurar a URL da API
// Ou você pode definir window.API_URL antes de carregar a aplicação

// Descomente e configure a URL do seu backend:
// window.API_URL = 'https://seu-backend.onrender.com';

// Se não configurar, a aplicação tentará usar URL relativa (mesmo domínio)

